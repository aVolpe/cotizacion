spring:

  flyway:
    enabled: true

  datasource:
    url: jdbc:h2:./data/db;DB_CLOSE_DELAY=-1;AUTO_SERVER=TRUE

  profiles:
    active: develop

  jpa:
    hibernate:
      ddl-auto: validate


  thymeleaf:
    cache: false
    prefix: classpath:/public/
#    suffix: html


logging:
  level:
    web: debug

